import React from 'react';
import { render } from '@testing-library/react';
import CommentsCard from '../components/graphic interface/CommentsCard';
import Comment from '../entities/Comment';
import Route from '../entities/Route';

test('test comments card', () => {
    /*const com1 = new Comment("comment1");
    const com2 = new Comment("comment2");
    const comms = [];
    comms.push(com1);
    comms.push(com2);
    
    const route = new Route("mockRoute", "mock description", [], comms, [], new Date()); 

    const { getByText } = render(<CommentsCard props={route}/>);

    const c1= getByText("comment1");
    const c2= getByText("comment2");

    expect(c1).toBeInTheDocument();
    expect(c2).toBeInTheDocument();*/
});